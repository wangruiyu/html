{
  "name": "html",
  "version": "1.0.0",
  "description": "static html project",
  "main": "index.js",
  "scripts": {
    "start": "anywhere -h localhost -p 8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "anywhere": "^1.5.0"
  }
}
